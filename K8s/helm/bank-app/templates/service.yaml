apiVersion: v1
kind: Service
metadata:
  name: {{ include "bank-app.fullname" . }}
  labels:
    app: {{ include "bank-app.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "bank-app.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      nodePort: {{ .Values.service.nodePort }}
      protocol: TCP
      name: http
